describe("set up function",()=>{
     
    beforeAll(()=>{
        //connectToDb()
    })

    afterAll(()=>{
        //disconnect()
    })

    beforeEach(()=>{
       // exclusive for this test file only
    })

    afterEach(()=>{
      // exclusive for this test file only
    })
    // instead of repaeting function , use setup functions ,beforeAll(),afterAll()
    // test("test one",()=>{connectToDb()})
    // test("test two",()=>{connectToDb()})
    // test("test three",()=>{connectToDb()})
})